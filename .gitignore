# Environment variables (local secrets only)
.env
.env.local
.env.*.local
# Production env files are safe to commit (they only contain public URLs)
# .env.production is intentionally NOT ignored

# Firebase service account (private key — never commit)
firebase-service-account.json
*-adminsdk-*.json

# Python
__pycache__/
*.pyc
*.pyo
venv/
.venv/

# Node / React
node_modules/
build/
dist/
.env.local

# macOS
.DS_Store

# IDE
.vscode/
.idea/
